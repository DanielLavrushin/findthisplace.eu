{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "site",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/site",
      "envFile": "${workspaceFolder}/.env",
      "args": ["--port", "8080"]
    }
  ]
}
